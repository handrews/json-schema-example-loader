{
  "id": "/fixtures/regression",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Regression Test Schema",
  "type": "object",
  "properties": {
    "str": {"type": "string"}
  },
  "additionalProperties": {
    "plus_one": {
      "description": "Tests for old merge-add'l-properties behavior."
    }
  },
  "links": [
    {
      "title": "Test bugs: required outside of schema",
      "href": "/fixtures/regressions",
      "method": "POST",
      "required": [
        "foo",
        "baz"
      ],
      "schema": {
        "required": ["boo"],
        "properties": {
          "foo": {"type": "boolean"},
          "baz": {"type": "boolean"},
          "boo": {"type": "boolean"}
        }
      }
    }
  ]
}
